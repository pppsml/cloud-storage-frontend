import { action } from "../types"

const initState = {

}

const fileReducer = (state = initState, action:action) => {
  switch (action.type) {
    default :
      return state
  }
}

export default fileReducer